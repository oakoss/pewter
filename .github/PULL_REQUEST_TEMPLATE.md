<!-- Delete these comments before submitting — this body becomes the
     mainline commit message on squash-merge, so anything left in it
     lands in permanent history. Write for someone searching git log in
     a year with a faint memory and no context: the diff shows WHAT
     changed; this carries what the diff can't show.

     The PR title becomes the commit subject and must satisfy
     commitlint: type(scope): subject.

     GitHub rewraps the body at ~72 columns, which flattens multi-line
     bullets — keep bullets to one line or write prose.

     Don't include: test counts or check statuses (the checks tab shows
     them), review-process recaps, bullets restating the diff, or
     Claude session links. -->

## Summary

<!-- The problem first, as an observable symptom — then the change and
     why this approach. Reference issues: "Closes pw-xxx." -->

## Notes

<!-- Only what earned its place: why not the obvious alternative,
     accepted limitations, review findings dismissed with the evidence,
     the docs/manual-testing.md items that cover this, follow-up issues
     filed. Delete this section if there's nothing non-obvious. -->
